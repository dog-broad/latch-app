/**
 * placeholder where the live encryption demo lands in subsequent
 * commits — the two boxes, the cipher strip, the real aes-gcm output.
 * for now: a sharp-edged outlined box that signals "demo coming here"
 * without faking the content. removing this stub is the visible
 * before/after of the hero-demo work.
 */
export function DemoSlot() {
  return (
    <div
      class="border border-border bg-bg-sunk text-fg-faint text-12 text-center px-6 py-16 rounded-sharp"
      aria-label="live encryption demo placeholder"
    >
      [hero demo]
    </div>
  )
}
